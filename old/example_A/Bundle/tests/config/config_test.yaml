framework:
  test: true
  secret: test
  router:
    utf8: true

monolog:
  handlers:
    main:
      type: stream
      path: "%kernel.logs_dir%/test.log"
      level: debug

services:
  _defaults:
    autowire: true
    autoconfigure: true
    public: true

  VLM\TaskWorkerBundle\Tests\Fixtures\TestTask:
    tags: ['taskWorker.task']
    arguments:
      $schedule: '* * * * *'
      $logger: '@logger'

  logger:
    alias: 'monolog.logger'
    public: true